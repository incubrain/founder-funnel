# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Node compile cache — populated by tools that opt into Node 22+'s
# v8 compile cache via NODE_COMPILE_CACHE. Regeneratable; can hit
node-compile-cache/

# Logs
logs
*.log

# Stale dev-server sockets (nuxt-vite-node / nitro-worker)
*.sock

# Misc
.DS_Store
.fleet
.idea
.eslintcache
repomix-output.*
repomix-changed.*
**/repomix-output.*

# Local env files
.env
.env.*
!.env.example

# Claude Code local overrides
CLAUDE.local.md
.claude/settings.local.json
.claude/visual-test-log.md

# Agent skills
# Custom skills live in /skills/ (committed to git)
# External skills installed to .agents/skills/ (gitignored, manifest in .claude/skills.json)
# Agent directories (.claude, .cursor, etc.) symlink to .agents/skills/
.agents/skills
.cursor/
.trae/
.windsurf/

# Documentation review comments (dev-only)
.comments/

# Misc
strategy
ideas
backups
trash
tree.txt
**/*-copy.md
**/* copy.md

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
